set(CURRENT_DIR ${CMAKE_CURRENT_SOURCE_DIR})

set(SOURCE_FILES

    ${CURRENT_DIR}/App.cpp

    ${CURRENT_DIR}/CMakeLists.txt
)

set(VKL_CORE_SOURCE_FILES ${VKL_CORE_SOURCE_FILES} ${SOURCE_FILES} PARENT_SCOPE)
